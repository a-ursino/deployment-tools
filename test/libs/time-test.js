import test from 'blue-tape';
// import { stub } from 'sinon';
// import { getDateString, format } from '../../src/libs/time';

/** @test {time} */
test('time', assert => {
	// assert.equal(typeof format, 'function');
	// assert.equal(typeof getDateString, 'function');
	// assert.equal(format(new Date(2016, 5, 19, 10, 0, 0)), '10:00:00', 'should be private');
	assert.end();
});
